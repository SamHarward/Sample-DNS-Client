Code is in Python 3.7, only dependencies required are socket and os.

After running code, use "my-dns-client <host-name>" to do a DNS query of <host-name> on server 8.8.8.8, or you can specify the DNS server with "my-dns-client <host-name> <dns-server>".

You can also use the "exit" command to end the program.
